export function addNumbers(a: number, b: number): number {
  return a + b;
}

export function formatStudentGreeting(name: string): string {
  return `Hello, ${name}! Welcome to the NOVA AI Internship.`;
}
